describe('All tests in here will pass', () => {
  it('It will pass', () => {
    expect(true).to.be.eq(true)
  })

  it('It will pass', () => {
    expect(true).to.be.eq(true)
  })

  it('It will pass', () => {
    expect(true).to.be.eq(true)
  })
})
